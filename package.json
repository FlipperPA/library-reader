{
  "name": "library",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js",
    "package": "node build.js && zip -r library-reader.zip manifest.json background.js assets/style.css assets/dist/content.bundle.js assets/icons/ assets/fonts/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@mozilla/readability": "^0.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
